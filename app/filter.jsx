import { View } from "react-native";

export default Page = () => {
    return (
        <View>
            {/* <Stack.Screen name="filter" options={{ title: 'Filter', presentation: "modal" }} /> */}
        </View>
    );
};
